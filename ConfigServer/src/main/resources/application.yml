server:
  port: 8085

spring:
  application:
    name: CONFIG-SERVER

  cloud:
    config:
      server:
        git:
          uri: https://github.com/muralikrishna8055/rating-microservice-config
          clone-on-start: true